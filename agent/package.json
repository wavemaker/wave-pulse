{
  "name": "@wavemaker/wavepulse-agent",
  "version": "1.0.0",
  "description": "Debug Agent that needs to installed in WaveMaker React Native app.",
  "main": "build/index.js",
  "scripts": {
    "test": "npm run build && jasmine",
    "build": "tsc && node ./scripts/build.js post-build",
    "start": "npm run build && node ./build",
    "publish": "tsc && node ./scripts/build.js post-build --production=true"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "@types/jasmine": "^5.1.4",
    "axios": "^1.7.4",
    "execa": "5.0.0",
    "fs-extra": "^11.2.0",
    "jasmine": "^5.2.0",
    "tar": "^7.4.3",
    "typescript": "^5.5.4",
    "yargs": "^17.7.2"
  }
}
